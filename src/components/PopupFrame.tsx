import * as React from 'react'

import './PopupFrame.scss'
const PopupFrame = (porps: { left?: number; top?: number }) => {
  return <div className="popup-frame">PopupFrame</div>
}

export default PopupFrame
